/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns:[
            {hostname: "tangible-ptarmigan-877.convex.cloud"}
        ]
    }
};

export default nextConfig;
